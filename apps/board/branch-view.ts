import {uiIcon} from './ui-icons.js';
import {INDUSTRY_PRODUCTS,SYSTEMS} from '../../src/game/model/industry.js';
import {productName} from '../../src/game/systems/industry-products.js';
import type {SessionObservation} from '../../src/runtime/session.js';
import {ERA_NODES,BRANCH_NODES,BRANCH_PATHS,BRANCH_PRODUCTS} from '../../src/game/model/branches.js';
import {ALL_PRODUCTS,SUBJECT_NAMES} from '../../src/game/systems/economy-catalog.js';
import {esc,inputsText} from './economy-view.js';
export const branchRequirements=(ids:string[]|undefined)=>ids?.map(id=>[...BRANCH_NODES,...ERA_NODES].find(n=>n.id===id)?.name??id).join(' + ')||'无';
export function branchPage(g:SessionObservation['game'],button:(id:string)=>string,selectedId=''):string{
 const b=g.economy!.branchView!;
 const ready=b.nodes.filter(n=>!n.known&&!n.missing.length),locked=b.nodes.filter(n=>!n.known&&n.missing.length),known=b.nodes.filter(n=>n.known);
 const selected=b.nodes.find(n=>n.id===selectedId)??ready[0]??known[0]??b.nodes[0];
 const glyph=(n:typeof selected)=>uiIcon(n.id==='A1'?'drop':n.subject==='agronomy'?'plant':n.subject==='materials'?'layers':n.subject==='mechanics'?'motion':'people');
 const state=(n:typeof selected)=>!n.active?(n.recorded?'历史已学 · 本阶段停用':'其他社会课程'):n.known?'已掌握':n.missing.length?'待前置':'可研习';
 const tile=(n:typeof selected)=>{const offer=g.actions.find(a=>a.id==='economy:branchlearn:'+n.id);return `<button type="button" class="lesson-tile ${selected.id===n.id?'selected':''} ${n.known?'mastered':''}" data-course="${n.id}" aria-pressed="${selected.id===n.id}"><span class="lesson-symbol">${glyph(n)}</span><span class="lesson-state">${n.known?'✓':n.missing.length?'⌑':'○'} ${state(n)}</span><strong>${n.name}</strong><small>${n.known?SUBJECT_NAMES[n.subject]:n.missing.length?'前置 '+n.missing.length+' 项':`时间 ${offer?.time??0}　精力 ${offer?.energy??0}`}</small></button>`;};
 const detail=`<aside class="course-inspector"><div class="inspector-head"><span class="inspector-symbol">${glyph(selected)}</span><span>${SUBJECT_NAMES[selected.subject]}<small>${state(selected)}</small></span></div><h3>${selected.name}</h3><p>${esc(selected.benefit)}</p>${selected.epigraph?`<blockquote>${esc(selected.epigraph)}</blockquote>`:''}<p>${selected.scope}${!selected.active?' · 本社会不生效；保留历史记录，不授予跨阶段加成':''}</p><div class="prerequisite-chain"><span class="detail-label">前置知识</span>${selected.parents.length?selected.parents.map(id=>{const n=b.nodes.find(x=>x.id===id)!;return `<button type="button" data-course="${id}" class="prerequisite ${n.known?'met':''}">${n.known?'✓':'○'} ${n.name} →</button>`;}).join(''):'<span class="met">✓ 无前置要求</span>'}<span class="chain-arrow">↓</span><strong>${selected.name}</strong></div><div class="inspector-action">${!selected.known?button('economy:branchlearn:'+selected.id):`<div class="learned-stamp">✓ 已掌握</div>${!selected.archived?button('economy:brancharchive:'+selected.id):''}${!selected.heirKnown?button('economy:branchteach:'+selected.id):''}`}</div><details><summary>传承与学习说明</summary><p>${selected.inherited?'前代已学，本人享传承减费。':'学习花费时间与精力，不消耗样品。'}后辈${selected.heirKnown?'已掌握':'未掌握'}。学会的课程会列在这里，集齐数量不是过关条件。</p></details><button type="button" class="inspector-rules" data-guide="学科">学堂完整规则</button></aside>`;
 return `<div class="school-toolbar"><div class="knowledge-count">${uiIcon('book')}<strong>${known.length}<small> / ${b.nodes.length}</small></strong><span>已学会的课程</span></div><span class="subtle">选择课程查看用途与费用。后续课程按前置开放，不是集齐即可胜利。</span></div><div class="school-workbench"><section class="course-library"><div class="library-heading"><h3>可以开始</h3><span>${ready.length} 门满足知识前置</span></div><div class="lesson-grid">${ready.map(tile).join('')||'<p>暂无新的可学课程。</p>'}</div><details class="school-collection"><summary>后续课程 · ${locked.length}</summary><div class="lesson-grid">${locked.map(tile).join('')}</div></details><details class="school-collection"><summary>已学会 · ${known.length} · 记录与教导</summary><div class="lesson-grid">${known.map(tile).join('')}</div></details><details class="school-collection"><summary>学习路线</summary>${b.paths.map(p=>`<article class="family-item"><h3>${p.name}</h3><progress class="tower-progress" aria-label="${p.name}已学会 ${p.learned} / ${p.nodes.length}" value="${p.learned}" max="${p.nodes.length}"></progress><p>${branchRequirements(p.nodes)}</p></article>`).join('')}</details><details class="school-collection"><summary>渠道与家族工艺</summary>${['electric','metal'].map(id=>b.channels.includes(id)?`<p>${id==='electric'?'电工材料商':'稳定金属供货'}：已联络</p>`:button('economy:channel:'+id)).join('')}<p>已试制规程：${esc(b.protocols.map(id=>g.economy!.processes.find(p=>p.id===id)?.name??id).join('、')||'暂无')}</p></details></section>${detail}</div>`;
}
export function branchCatalog(products=false):string{
 return `<section class="card"><h2>0.27.0 · 文明阶段、社会服务与自选结算</h2><p>力学、材料、农学、组织从根节点分叉。部分课程随社会发展开放：农业是栽培与储存，集镇是成形与泵，工业是发电，现代是配电。社会服务不靠点树：公地采食、集镇公井、公共磨坊、市政自来水。玩家随时可结算当前社会。当前系统包括${SYSTEMS.map(s=>s.name).join('、')}。学习不消耗样品。</p>${BRANCH_PATHS.map(p=>`<p>${p.name}：${branchRequirements(p.nodes)}</p>`).join('')}</section>`+(products?ALL_PRODUCTS.filter(p=>BRANCH_PRODUCTS[p.id]).map(p=>`<section class="card"><h3>${p.name}</h3><p>${p.effect}</p><p>知识：${branchRequirements(BRANCH_PRODUCTS[p.id])}；前置产品验证：${(INDUSTRY_PRODUCTS.find(x=>x.id===p.id)?.parents??[]).map(productName).join(' + ')||'无'}</p><p>材料：${inputsText(p.inputs)}</p></section>`):[...BRANCH_NODES,...ERA_NODES].map(n=>`<section class="card"><h3>${n.id} ${n.name}</h3><p>前置：${branchRequirements(n.parents)}；只依赖前置知识，学习花时间与精力。</p><p>${n.benefit}</p></section>`)).join('');
}
