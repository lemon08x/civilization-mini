# 规则预研约定

## 模型试玩入口

- 用户要求“玩游戏、试玩几局、玩后说感受”时，先阅读 `playtests/AGENTS.md` 和 `playtests/START_HERE.md`，按独立批次保存试玩记录与感受。
- 默认由当前模型根据每次观察逐步决定行动。不得玩一局后编写或运行自动决策脚本代跑剩余局数。
- 试玩任务与感受写入 `playtests/batches/`；原始游戏存档仍在 `artifacts/runs/`。试玩发现不自动转为规则修改。

## 项目边界

- 本目录是独立预研项目，不修改 ../remake，不引入 Cocos 或正式游戏的剧情、战斗、美术工程。
- 当前只维护规则 0.27.0。不保留旧规则 JSON、旧实验归档、研究推导或脚本策略模块。
- src/game/game.ts 是唯一结算权威。网页、命令行和大模型必须通过同一行动接口，不能直接修改状态。
- 策略只能读取 observeSession() 输出；种子、随机状态和未来天气不得进入观察。大模型文案不决定物理结果。
- 默认只做相关单元测试和短时桌面试玩。不运行千局实验，不创建常驻 AI 循环或付费 API 调用。
- 读取失败时不覆盖原档。当前局写在 artifacts/runs/，不入库。
- 领域层不依赖 runtime 或 apps。玩家只用 scripts/player.mjs 的 observe / act。
- 默认在当前分支工作；只有用户明确要求时才创建或切换分支。
- 验证按改动影响精简：开发中只运行相关测试，一轮代码改动收尾统一运行一次 npm test。纯文档、说明或格式修改不触发构建、测试或模拟。界面行为/布局变化再做一次短时试玩。
