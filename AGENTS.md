# 规则预研约定

## 游戏主线与当前开发重点（2026-09-24，优先于旧主线）

项目核心是**设计一个人的全年计划**：玩家逐步定好每一天的行为，通过科技提高行为收益与效率，在有限时间和资源约束下形成能稳定运行、趋于生产力上限的方案。完成的计划自动运行一轮，以实际计划产值评价。

整体成绩按四个阶段的计划产值及其占比计算，越后期占比越高；支持只玩一个阶段刷高分，也支持平衡各阶段收益、追求整体得分。具体权重、产值折算和循环稳定性判定尚未确定，不得自行把讨论示例固化为规则数字。

当前只专注农业阶段，种地与需要时间维护、升级的修行是已确定的计划方向。修行课程树、日课、精修与师徒传习已实现，真实消耗时间；课程效果按用户要求留空，不提供旧五境、心法或卡牌加成。后续目标仍是给人物全局行动提供加成，具体效果与公式待设计。休息与口粮、采集、堆肥与加工、学习和建设仍是待评估的辅助行为，不视为全部获准实现；后续三个阶段暂不展开。

新主线替换旧的“修道习术—师徒传承—现代危机唯一计分”玩法与评分要求及“各阶段无限发展”要求，**保留门派背景与传承关系作为跨四时代个人持续发展的叙事基础**。已有相关代码不表示旧评分目标继续有效；按后续具体任务调整，不因文档更新擅自删除机制。详细方向见 [README.md](README.md)，实施顺序见 [docs/PR3_IMPLEMENTATION_PLAN.md](docs/PR3_IMPLEMENTATION_PLAN.md)。旧文档与本节冲突时，以本节及用户最新指示为准。

剧情与美术延续门派生活的轻松质感，通过学艺、同门往来、季节与器物增加趣味，减轻现实生计和绩效的代入压力。不要把全年计划低分叙述为辜负师门，也不再把现代灭绝危机设为剧情的必然归宿。跨时代已确定保留师徒接续，让玩家的经历沿传承延续；不另行引入同一人物长生或轮回的解释。现有寿命和交接规则的具体调整须按机制任务实施。

全年计划自动执行和四阶段新评分尚未实现；说明、界面与开发反馈必须区分已定设计和已实现机制。后续新增行为或科技应说明它如何改变全年时间安排或计划产值，不以扩张无关成长系统为目标。

## 试玩记录新约定（优先于下文旧记录流程）

不保存或提交试玩记录。试玩任务、观察、停止原因与感受只在对话中反馈，不创建批次目录、笔记、观察快照或报告，不复制或填写试玩模板。游戏运行所需的 `saves/` 存档仍由现有入口管理，不入库。下文涉及保存批次记录的旧要求不再执行。

## 保留范围

当前专注桌面 Web 开发与体验优化，暂不考虑手机端。不主动新增手机适配、移动端布局或窄屏验收；界面核验以桌面浏览器为准，除非用户另行明确要求。

本仓库只维护这些部分：

- 游戏机制：`src/game/`（结算权威是 `src/game/game.ts`）
- 现行规则：`rulesets/`（当前仅 0.27.0）
- 用户界面：`apps/`（网页与命令行入口）
- AI 试玩规则：`playtests/AGENTS.md`、`playtests/START_HERE.md`、`playtests/templates/`

为让界面能跑，现有 `src/runtime/` 继续作为会话胶水，不在此列扩张。AI 入口和精简观察在 `playtests/`。

## 新增需确认

保留范围之外的**新增**内容必须先得到用户明确同意，包括但不限于：

- 新目录、新模块、新脚本、新文档、新规则版本、新测试文件、新实验
- 在 `playtests/` 增加 `player.mjs` 以外的新入口
- 在 `tests/` 增加用例
- 新增或扩展 `docs/`、研究层、策略层、旧版本规则

改游戏机制、规则 JSON、界面或试玩约定本身，按用户当次任务执行，不必再确认“是否属于保留范围”。

## 改机制或改数值

先判定改的是哪一类，不要混着做：

| 类型 | 含义 | 典型 |
| --- | --- | --- |
| 机制 | 因果关系变了 | 新行动、新状态、季末顺序、传承是否保留 |
| 数值 | 同一公式换数字 | 工资、产量、电灯加时、购粮额度 |
| 呈现 | 世界结果不变 | 文案、排版、compact 摘要 |

共同规则：用一两句话写清玩家看到的问题；试玩感受只在对话中反馈，不自动改规则；不新开目录、测试或文档，除非已确认。

### 改机制

1. 写清因果链：玩家能看见什么 → 能做什么 → 当季/季末世界如何变 → 换代保留什么。
2. 优先接到现行模块（经济、学科、工业、人生、时代、购粮、商城、供能），不要先做新子系统。
3. 实现顺序：`model` 类型 → `systems` 过程（含 `time.ts` 季末顺序）→ `actions` → `observation.ts` → 若有新数字再改 `rulesets/v27` 与 bounds → 人类要点则改 `apps/board` → AI 要看见则改 `playtests/compact-observation.ts`。
4. 只通过 `game.ts` 的 `transition` 改世界。网页和 `player.mjs` 不得直接改状态。
5. 状态形状变了：不迁移旧档。当前加载器只接受 `0.27.0`；若要升版本，先问再改校验与 JSON。
6. 机制被接受后，同步 [docs/CURRENT_GAMEPLAY_V27.md](docs/CURRENT_GAMEPLAY_V27.md) 里受影响的段落。
7. 不主动跑测试。界面或布局变了再短时点一下相关页。

### 改数值

1. 先找数字在 JSON 还是 TypeScript 目录（见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)「数字在哪」）。
2. 在 `rulesets/v27` 里：参数改 `parameters.json` / `systems.json`；物价、产量、配方投入改 `catalogs.json`。若要超出 bounds，先确认再扩边界。
3. 名称、效果和节点结构仍在 TypeScript 目录；不要只为改一个数字去改机制代码。
4. 一次只动一组相关数字。写假设（预期哪条因果变）和要看的副作用（粮、时间、钱、传承）。
5. 公式不对就走「改机制」，不要用改公式去迁就一个数字。
6. 用短时试玩或用户指定的测试核对；不跑千局、不建常驻调参循环。
7. 数值被接受后，只改玩法文档里实际写到的数字。

## 测试

- 用户未明确要求跑测试或验证时，不运行测试，只做与改动直接相关的简单检查；不为收尾追加全面验证、回归检查或重复试玩。此条优先于本文其他验证要求。
- 不主动运行 `npm test`、单项测试，也不为收尾补跑测试。
- 只在用户明确要求时，才运行 `tests/` 中的测试（或用户指定的那一部分）。
- 纯文档、说明或格式修改不构建、不测试、不模拟。界面行为或布局变化再做一次短时试玩。

## 模型试玩入口

- 用户要求“玩游戏、试玩几局、玩后说感受”时，先阅读 `playtests/AGENTS.md` 和 `playtests/START_HERE.md`，按独立批次试玩，感受只在对话中反馈。
- 默认由当前模型根据每次观察逐步决定行动。不得玩一局后编写或运行自动决策脚本代跑剩余局数。
- 试玩任务与感受不写入文件。命令行存档在 `saves/`，网页存档在浏览器开始界面打开或新开。试玩发现不自动转为规则修改。

## Grok Build 调用经验（2026-09-24）

- 用户在 PowerShell 中直接运行 `grok`，网络使用系统 Clash 代理。自动化启动的进程不一定继承相同代理环境；不能仅凭该进程直连超时就判断 Grok 服务或用户网络不可用。
- 调用前检查当次进程的 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 与系统现有代理是否一致。必要时仅向当次 PowerShell 进程传入已配置的代理，并保留 `NO_PROXY=localhost,127.0.0.1,::1`；不要修改全局设置，不要假定所有机器都使用同一端口。本机本次成功使用 `http://127.0.0.1:7897`。
- Grok 配置的 `shell_environment_policy.set` 和 `web_fetch.proxy_endpoint` 不等于 Grok 主进程的模型请求已继承代理。此次连接 `cli-chat-proxy.grok.com/v1/responses` 超时，在启动进程显式传入 Clash 代理后恢复，`image_gen` 成功生成图片。
- 生图工具可能将图片写到 `.grok/sessions/.../images/`，按工具实际返回路径复制原图进项目，再裁切导出。以图片文件和工具完成结果判断成功，不只看 CLI 最终退出码；素材来源与提示词记录在 `apps/board/illustrations/README.md`。

## 项目边界

- 本目录是独立预研项目，不修改 ../remake，不引入 Cocos 或正式游戏的剧情、战斗、美术工程。
- 当前只维护规则 0.27.0。不保留旧规则 JSON、旧实验归档、研究推导或脚本策略模块。
- `src/game/game.ts` 是唯一结算权威。网页、命令行和大模型必须通过同一行动接口，不能直接修改状态。
- 策略只能读取 observeSession() 输出；种子、随机状态和未来天气不得进入观察。大模型文案不决定物理结果。
- 不运行千局实验，不创建常驻 AI 循环或付费 API 调用。
- 读取失败时不覆盖原档。命令行当前局写在 `saves/`，不入库。
- 领域层不依赖 runtime 或 apps。AI 玩家只用 `playtests/player.mjs` 的 observe / act。
- 默认在当前分支工作；只有用户明确要求时才创建或切换分支。
