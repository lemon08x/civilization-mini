# 规则预研约定

## 模型试玩入口

- 用户要求“玩游戏、试玩几局、玩后说感受”时，先阅读 `playtests/AGENTS.md` 和 `playtests/START_HERE.md`，按独立批次保存试玩记录与感受。
- 默认由当前模型根据每次观察逐步决定行动。不得玩一局后编写或运行自动决策脚本代跑剩余局数；用户明确要求脚本基线时才采用脚本模式，并单独标注。
- 试玩任务与感受写入 `playtests/batches/`；原始游戏存档仍在 `artifacts/runs/`。试玩发现不自动转为规则修改或批量研究。

## 项目边界

- 本目录是独立预研项目，不修改 ../remake，不引入 Cocos 或正式游戏的剧情、战斗、美术工程。
- 先维护世界过程、可观察状态、行动及传承的因果关系，再调整参数；所有初始数值是待检验假设。
- src/game/game.ts 是唯一结算权威。网页、脚本策略、大模型必须通过同一行动接口，不能直接修改状态。
- 规则变更要递增规则版本；参数候选单独保存。比较实验使用相同场景、种子与策略，不能混用历史版本存档。
- 策略只能读取 observeSession() 输出；种子、随机状态和未来天气不得进入观察。大模型文案不决定物理结果。
- 脚本策略不称为大模型 AI。没有真实调用大模型时，报告明确写出使用的是脚本基线。
- 不自动采纳 AI 提议：先记录假设，再做小批量对照，查看轨迹和副作用，最后决定是否接受。
- 默认只做相关单元测试、少量固定种子对照和短时桌面试玩。不运行千局实验，不创建常驻 AI 循环或付费 API 调用。
- 保留实验、存档和用户修改。不在读取失败时覆盖原档。

- 领域层不依赖 runtime、agents、research 或 apps；运行层不依赖策略与指标。统计由事件推导，不写入 GameState。
- 不修改 tests/fixtures/legacy-v1.json 来让失败测试通过；它是重构前的独立基准。旧版迁移要求冻结规则指纹。
- 玩家只用 scripts/player.mjs 的受限命令；研究修改写 experiments/，不自动覆盖 rulesets/。规则变更必须附带版本与迁移方案。
- 默认在当前分支工作；只有用户明确要求时才创建或切换分支，不因开发、规则变更或备份需要自行建分支。
- 验证按改动影响精简：开发中只运行相关测试，一轮代码改动收尾统一运行一次 npm test（含 8 条旧轨迹与接口保护检查）；纯文档、说明或格式修改不触发构建、测试或模拟。界面行为/布局变化再做一次短时试玩。
- 不为报告与最终实现指纹一致而重跑实验。报告保留实际运行时的规则、实现指纹和结论范围；后续改动未影响相关结算、策略或实验条件时，继续引用原报告，必要时简短注明适用范围，不刷新报告或迁移存档来追齐指纹。
- 只有明确的研究问题，或行为变化/失败使已有结论需要复核时，才运行最小必要的固定种子对照；优先复核受影响的场景与策略，不默认重跑全批或扩大规模。纯校验、文案、格式及无行为变化的整理，不重复模拟。
- 精简验证不等于放宽存档重放校验：原档和原报告保留，不伪造新指纹，不把旧实验冒充当前版本的新结果。
