# v0.2.0 重构验收记录

日期：2026-09-10。规则参数与平衡不调整。

- `npm test`：12 项通过，包含类型编译。
- 冻结旧实现的 8 条轨迹共 349 个逐步状态一致；包括两地区、四种脚本策略、候选实践费用、生活缺口提前结束。
- 每条轨迹验证 v2 重放与 v1 显式迁移，比较资源、天气、可用行动与费用、个人/后辈成长、设施、项目、代际记录、统计和指标。
- 命令幂等、过期拒绝、非法行动、冻结输入、观察隔离；配置/事件链状态哈希/快照/实现身份不一致拒绝；文件历史备份、禁止覆盖与路径穿越检查通过。
- 实际 CLI 新建与受限玩家入口的一步学习通过；独立运行了同种子、同场景、同策略的基线/候选两局冒烟实验，报告位于 `artifacts/experiments/refactor-smoke-*`。
- 本地 HTTP 冒烟验证 27 项页面资源和完整浏览器模块依赖图可访问，不包含 Node 专用依赖；文件存储模块不可通过服务器访问。
- 实际浏览器点击试玩未完成：CUA 工具报 `unsupported Codex auth method: apikey`。HTTP 检查和类型检查不替代点击、刷新与导入的真实浏览器验证。

原代码保存于 Git 标签 `baseline/rules-lab-0.1.0`，完整备份位于 `../civilizationMini.backups/before-refactor-20260910-163015.zip`。旧 `runs/`、`reports/` 保留。旧引擎、旧入口与其测试已由新模块和基准检查替代，可从标签查看。
