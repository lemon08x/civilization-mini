# 对 Grok 理论倍率实验的独立复核

2026-09-11，复核者 Codex。保留 Grok 原报告、提案、脚本与实验产物；本文件是独立审阅，不采纳参数、不修改实验结论状态。

结论：支持本轮拒绝将默认 studyMultiplier 从 1 提为 2。8 局数据可靠，3/4 配对路线损失已继承节点，拒绝候选并按预设条件停止留出合理。这只支持当前种子17、两场景和两脚本下的局部决定，不能证明默认1已是全局最优。

## 实际核验

读取 artifacts/experiments/study-multiplier-20260911-2026-09-11T05-37-41-390Z-51a1349a。独立对全部8个逐局文件执行 replayRecord，均通过完整校验；从重放结果重新计算 metrics，与 metrics.json 逐项一致；每个原文件 SHA-256 与 research-evidence.json 一致，策略文件指纹按原报告算法核对一致。全部49命令、complete，没有新增实验或重新运行策略。

清单唯一候选 studyMultiplier=2，其余条件一致。当前 tracked 文件无改动，Grok新增文件在 experiments/，模拟产物在 artifacts/experiments/；默认参数仍为1。没有运行留出目录，只有预先准备的留出计划。模型身份仅能依据用户说明与报告声明，存档实际控制器为scripted，不能从存档独立认证Grok模型版本或工具推理设置。

## 需要修正的报告问题

1. **补足林地陶作增产的材料因果（原分析第23、27行）。** 基线 run-0001 revision24 的 teach:pottery 实践真实扣 wood1、clay1。林地一共只有4黏土且不再生；基线首批用2、教学用1、终点剩1，不足以再开工。候选 run-0005 没有陶作实践教学，revision34采到第二份2黏土、37将其投入第二批，39完工。因此产量2→4不仅是“第二代重新学习”，更与取消教学材料支出有关。确有新增可销售产品，但同时牺牲传承，不能把产品本身称为无效重复投入。这是“教学材料成本与非再生资源约束发生冲突”的研究线索，尚不能直接断言教学机制应修改。

2. **制作行动统计口径不完整（原分析第14—21行）。** 表格统计的是 craft 开工次数，没有包含 finish-craft。若列名保留“制作行动”，应使用开工+完工：林地陶作2→4、林地混合16→14、河谷陶作8→5、河谷混合8→6。也可保留原数字但将列名改为“开工次数”，另列完工次数。不能把当前数字用于比较总制作行动成本。

3. **提案过度概括教导次数（proposal.result）。** “陶作路线教导降至1次”只适用于两个 potter 配对；河谷 mixed 同样走陶作，但总教导是5→4，其中陶作教导3→1、控火教导2→3。应区分策略、总教导与单节点教导，保持提案和分析表一致。

## 研究脚本的后续改进

check.mjs 在实现指纹不匹配时记录 verified:false 后继续，末尾 verification 却固定写“records fully replayed”，进程也正常退出。此次8份均匹配，因此不影响本轮结果；未来混入不匹配文件会产生矛盾状态。建议遇到不匹配就停止，或把总体状态明确设为partial/failed并返回非零退出码，保留原文件且不写“全部校验”。这是流程健壮性问题，本次未执行该失败路径，不修改原脚本。

## 下一研究的方向

本轮证明学习负担对有限代际窗口敏感，没有产出更优参数。下一轮应先核查林地的黏土守恒和教学实践是否带来合理的取舍：分别观察材料投入、后辈技能、实际产品以及跨代用途。先提出新假设，再选择策略对照或单项机制候选；不要同时改学习倍率、教学成本和资源恢复，也不要为了让传承指标上升直接取消实践成本。

本次仅复核和新增审阅文档，没有构建、全套测试、付费调用、模拟批次或Git提交/推送。

## 后续修复（用户授权后，2026-09-11）

上述三处报告/提案问题已修正；原版本另存 experiments/history/study-multiplier-20260911-before-correction/。校验脚本现在遇到指纹不匹配即抛错并非零退出，空目录也拒绝出具报告；只有全部记录通过后才独占创建输出。未改写既有 research-evidence.json、逐局记录或其指纹。

新增三项进程级检查覆盖混合指纹失败、空目录失败、成功写出及拒绝覆盖；收尾 npm test 共48项通过。原始8份逐局文件的 SHA-256 与既有证据逐一核对未变。本次修复没有新模拟批次，没有改动默认参数、规则版本或领域实现指纹。上文问题描述保留为修复前审阅历史。
