# 工艺成长联动探针（执行前假设）

问题：在规则 0.5.0 默认资源、每季 3 行动、每代 16 季下，农业能否加工补给并用于实验和家族改良；陶作能否离开容器生产进入构件；后代换行业能否实际使用留下的成果？

运行：固定种子 17，林地与黏土河谷。每个场景各一条针对成长机制的脚本和一条原有 subsistence 脚本，共 4 局，每局最多 160 命令。所有选择只读 observeSession，执行只用 submitCommand。不是大模型 AI，不修改默认参数。

成功证据：实际生产/实验/改良事件与跨代使用；同时报告缺粮、未完成目标和最终资产。不以脚本未覆盖证明规则不可达，不以四局证明数值平衡。每份轨迹保留实际规则、实现、脚本指纹并严格重放。只生成新的实验目录，不覆盖历史记录，不自动采纳候选。
